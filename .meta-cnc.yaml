# ---------------------------------------------------------------------
# NOTE: This file has been automatically generated with SkilletStamper
# to get the complete information and doc on what is done here go to
# 'https://github.com/PaloAltoNetworks/panhandler/blob/develop/docs/metadata_configuration.rst'
#
# ---------------------------------------------------------------------
# skillet preamble information used by panhandler
# unique snippet name
name: TAPModePoC
# label used for menu selection
label: TAP Mode PoC
description: Generic config for TAP mode to start a PoC
type: panos
extends:
labels:
  collection:
    - MobileSkillet
# end of preamble section
# ---------------------------------------------------------------------
# variables section
# SkilletStamper is to avoid exhaustive variables sections and produce hardcoded
# XML snippets instead based on an excel sheet. You can however use variables here
# and edit/change the values in PanHandler as you are used to. Mind, that for this
# case you have to ensure that your variables are inside the XML snippets
variables:
#
 - name: UEIPPool
   description: IP Pool for own subscribers UE
   default: 100.64.0.0/16
   type_hint: ip_address

 - name: SFN35ServerIP
   description: IP address of SFN3.5 Server
   default: 10.0.0.1/32
   type_hint: ip_address

 - name: TAPNIC
   description: Interface for TAP zone
   default: ethernet1/1
   type_hint: text
#
# ---------------------------------------------------------------------
# snippets section
# ---------------------------------------------------------------------
# snippets used for api configuration including xpath and element as file name
# files will load in the order listed
snippets:
#
 - name: Objects
   xpath: /config/devices/entry[@name='localhost.localdomain']/vsys/entry[@name='vsys1']/address
   file: AddrObject.xml

 - name: SFNSyslogSrv
   xpath: /config/shared/log-settings/syslog
   file: SFNSyslogSrv.xml

 - name: Interface
   xpath: /config/devices/entry[@name='localhost.localdomain']/network/interface
   file: TAPNIC.xml

 - name: Zone
   xpath: /config/devices/entry[@name='localhost.localdomain']/vsys/entry[@name='vsys1']/zone
   file: Zone.xml

 - name: Rules
   xpath: /config/devices/entry[@name='localhost.localdomain']/vsys/entry[@name='vsys1']/rulebase/security/rules
   file: RulesPOCTAPGeneric.xml

#- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# Static Section (no variables used in those xmls)
#- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
 - name: ZoneProtect_IPOnly
   xpath: /config/devices/entry[@name='localhost.localdomain']/network/profiles/zone-protection-profile
   file: ZoneProtectIPOnly.static.xml

 - name: GTPProfile
   xpath: /config/devices/entry[@name='localhost.localdomain']/vsys/entry[@name='vsys1']/profiles/gtp
   file: GTPProfileLogTunnel.static.xml

 - name: SCTPProfile
   xpath: /config/devices/entry[@name='localhost.localdomain']/vsys/entry[@name='vsys1']/profiles/sctp/entry
   file: SCTPProfileCommonS6SS7.static.xml

 - name: SFNSyslogProf
   xpath: /config/shared/log-settings/profiles
   file: SFNSyslogProf.static.xml
